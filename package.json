{
  "name": "pi-ecosystem",
  "version": "26.5.4",
  "private": true,
  "type": "module",
  "description": "Season's Pi extensions and skills published as npm packages.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present",
    "pack:worktree": "npm pack --workspace @season179/pi-worktree",
    "pack:skills-status": "npm pack --workspace @season179/pi-skills-status",
    "validate": "npm run build && npm pack --workspace @season179/pi-worktree --dry-run && npm pack --workspace @season179/pi-skills-status --dry-run"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.74.0",
    "@types/node": "^22.15.3",
    "typescript": "^5.9.2"
  }
}
