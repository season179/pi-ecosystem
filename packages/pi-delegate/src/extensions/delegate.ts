/**
 * pi-delegate extension entry point.
 *
 * Milestone 1: spawn layer only (../worker.ts) — nothing is registered yet.
 * Milestone 2 registers the `delegate` tool here (checkpoint, harness-run
 * verify, result contract). See DELEGATE.md at the repo root.
 */

import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function setup(_pi: ExtensionAPI): void {
	// Intentionally empty until milestone 2.
}
